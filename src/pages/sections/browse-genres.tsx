import { Library } from "lucide-react";
import { SectionPage } from "@/components/sections/SectionPage";

export default function BrowseGenres() {
  return (
    <SectionPage
      title="Browse by Genre"
      icon={Library}
      section="browse_genres"
    />
  );
} 